




<div class="page page--background">
    <header>
        {{> back-button}}
    </header>

    <form class="page__section">
        <h1 class="page__title">Відновлення паролю</h1>

        <div class="form">
            <div class="form__item">
                {{>field action="recoveryConfirmpForm.change" label="Код" type="number" name="code" placeholder="Ваш код"}}
                <span name="code" class="form__error">Помилка</span>
            </div>
            <div class="form__item">
                {{>field-password action="recoveryConfirmForm.change" label="Новий пароль" name="password" placeholder="Введіть пароль"}}
                <span name="password" class="form__error">Помилка</span>
            </div>
            <div class="form__item">
                {{>field-password action="recoveryConfirmForm.change" label="Новий пароль ще раз" name="password" placeholder="Введіть пароль"}}
                <span name="passwordAgain" class="form__error">Помилка</span>
            </div>
        </div>

        <button onClick="recoveryConfirmForm.submit()" class="button button-disabled" type="button">Відновити пароль</button>

        <span class="alert alert--disabled">Внимание, ошибка!</span>
    </form>
</div>